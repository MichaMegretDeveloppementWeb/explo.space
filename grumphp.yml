grumphp:
  tasks:
    composer: ~
    phpstan:
      configuration: phpstan.neon
    phpunit:
      config_file: phpunit.xml
      testsuite: null
      always_execute: false
    shell:
      scripts:
        - 'vendor/bin/pint -v'
